/**
 * Purpose: Server entry point.
 * Connects to the database and starts the HTTP server on the configured port.
 *
 * Inputs: Environment variables (PORT, MONGO_URI).
 * Outputs: Running HTTP server.
 * Acceptance Criteria: Server logs 'Listening on port X' and connects to MongoDB successfully.
 */

// Implementation will go here
